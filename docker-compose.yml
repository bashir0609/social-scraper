version: '3.8'

services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8888:8888"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - PORT=8888
    # Optional restart policy
    restart: unless-stopped
